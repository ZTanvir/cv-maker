const NameAndProfession = ({}) => {
    // const name =

    return (
        <section>
            <h2>{name}</h2>
            <p>{profession}</p>
        </section>
    );
};

export default NameAndProfession;
